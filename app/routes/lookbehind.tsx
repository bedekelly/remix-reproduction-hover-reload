import { lookbehind } from "~/file-with-regex";
console.log(lookbehind);

export default function LookbehindPage() {
  return (
    <>
      <h1>😈</h1>
      <button onClick={() => alert("yes!")}>Does JS work?</button>
    </>
  );
}
